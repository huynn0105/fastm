

class TimeFrameModel {
  TimeFrameModel({
    this.id,
    this.name,
    // this.timeFrom,
    // this.timeTo,
    // this.suffix,
    // this.type,
  });

  TimeFrameModel.fromJson(dynamic json) {
    id = json['ID'] ?? json['id'];
    name = json['name'];
    // timeFrom = json['timeFrom'];
    // timeTo = json['timeTo'];
    // suffix = json['suffix'];
    // type = json['type'];
  }

  int? id;
  String? name;
  // String? timeFrom;
  // String? timeTo;
  // String? suffix;
  // int? type;

  Map<String, dynamic> toJson() {
    final map = <String, dynamic>{};
    map['id'] = id;
    map['ID'] = id;
    map['name'] = name;
    // map['timeFrom'] = timeFrom;
    // map['timeTo'] = timeTo;
    // map['suffix'] = suffix;
    // map['type'] = type;
    return map;
  }

  String getTitle(/*{bool withSuffix = true}*/) {
    // String from = DateTimeUtil.convertDate(timeFrom, fromFormat: DateTimeFormat.HHmmss, toFormat: DateTimeFormat.HHmm, isFromUtc: false);
    // String to = DateTimeUtil.convertDate(timeTo, fromFormat: DateTimeFormat.HHmmss, toFormat: DateTimeFormat.HHmm, isFromUtc: false);
    // return '${withSuffix ? suffix : ''} $from - $to';
    return name ?? '';
  }
}
