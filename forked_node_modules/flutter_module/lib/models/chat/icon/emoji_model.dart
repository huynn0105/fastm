import 'package:flutter_module/models/chat/icon/icon_model.dart';

class EmojiModel extends IconModel {
  EmojiModel({
    super.code,
  });

  EmojiModel.fromJson(dynamic json) : super.fromJson(json);
}
