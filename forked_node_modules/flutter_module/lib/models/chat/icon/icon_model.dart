/// code : "Cat_12"

class IconModel {
  IconModel({
    this.code,
  });

  IconModel.fromJson(dynamic json) {
    code = json['code'];
  }

  String? code;

  Map<String, dynamic> toJson() {
    final map = <String, dynamic>{};
    map['code'] = code;
    return map;
  }
}
