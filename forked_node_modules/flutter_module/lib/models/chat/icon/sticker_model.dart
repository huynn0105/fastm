import 'package:flutter_module/models/chat/icon/icon_model.dart';

class StickerModel extends IconModel {
  StickerModel({
    super.code,
    this.asset,
  });

  StickerModel.fromJson(dynamic json) : super.fromJson(json) {
    asset = json['asset'];
  }

  String? asset;
}
