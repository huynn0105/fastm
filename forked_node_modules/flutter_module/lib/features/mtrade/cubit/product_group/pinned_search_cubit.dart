import 'package:bloc/bloc.dart';
part 'pinned_search_state.dart';

class PinnedSearchCubit extends Cubit<PinnedSearchState> {
  PinnedSearchCubit() : super(PinnedSearchState());

  changeCollapseValue(bool value) {
    emit(state.copyWith(isCollapse: value));
  }
}
