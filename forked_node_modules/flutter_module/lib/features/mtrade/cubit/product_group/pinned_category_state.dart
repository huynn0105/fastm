part of 'pinned_category_cubit.dart';

class PinnedCategoryState {
  final bool collapse;

  PinnedCategoryState({
    this.collapse = false,
  });

  PinnedCategoryState copyWith({
    bool? collapse,
  }) {
    return PinnedCategoryState(
      collapse: collapse ?? this.collapse,
    );
  }
}
