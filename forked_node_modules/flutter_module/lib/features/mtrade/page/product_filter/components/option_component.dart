import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:flutter_module/common/bloc_status.dart';
import 'package:flutter_module/common/dialogs/dialog_provider.dart';
import 'package:flutter_module/common/size.dart';
import '../../../cubit/filter/mtrade_filter_cubit.dart';

import '../../../../../common/colors.dart';
import '../widgets/button.dart';

class FilterActionComponent extends StatelessWidget {
  const FilterActionComponent({
    Key? key,
    required this.visible,
    required this.onClear,
    required this.onApply,
  }) : super(key: key);

  final bool visible;
  final Function() onClear;
  final Function() onApply;

  @override
  Widget build(BuildContext context) {
    return Visibility(
      visible: visible,
      child: Container(
        height: 48 + AppSize.instance.safeBottom,
        alignment: Alignment.topCenter,
        decoration: const BoxDecoration(
          color: UIColors.white,
        ),
        child: Row(
          children: [
            Expanded(
              child: FilterOptionButton(
                title: "Thiết lập lại",
                onTap: onClear,
                textColor: UIColors.primaryColor,
                backgroundColor: UIColors.lightBlue,
              ),
            ),
            Expanded(
              child: FilterOptionButton(
                title: "Áp dụng",
                onTap: onApply,
                textColor: UIColors.white,
                backgroundColor: UIColors.primaryColor,
              ),
            ),
          ],
        ),
      ),
    );
  }
}
