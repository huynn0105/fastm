import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/utils/log_util.dart';
import 'package:flutter_module/features/mtrade/page/order/order_process_state/items/order_process_state_mixin.dart';
import 'package:flutter_module/routes/routes.gr.dart';
import '../../../../../../../common/utils/format_util.dart';
import '../../../../../../../common/colors.dart';
import '../../../../../../../common/styles.dart';
import '../../../../../../../common/utils/text_util.dart';
import '../../../../../../../common/widgets/buttons.dart';
import '../../../../../../../common/widgets/images.dart';
import '../../../../../../../models/mtrade/order/order_model.dart';

class OrderSuccessProcessStateItem extends StatelessWidget with OrderProcessStateMixin {
  const OrderSuccessProcessStateItem({
    Key? key,
    required this.data,
    this.isDisable = false,
  }) : super(key: key);

  final OrderModel data;
  final bool isDisable;

  @override
  Widget build(BuildContext context) {
    final title = data.processText ?? "";
    final image = data.icon ?? "";
    final bonus = data.sumBonus ?? 0;
    final date = convertDate(data.updatedDate);

    return AppSplashButton(
      isDisable: isDisable,
      onTap: () {
        context.pushRoute(MTradeOrderDetailRoute(orderId: data.id));
      },
      child: SizedBox(
        height: 44,
        child: Row(
          children: [
            Align(
              alignment: Alignment.topLeft,
              child: AppImage.network(
                url: image,
                width: 24,
                height: 24,
              ),
            ),
            const SizedBox(
              width: 10,
            ),
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Row(
                    mainAxisAlignment: MainAxisAlignment.spaceBetween,
                    children: [
                      Text(
                        title,
                        style: UITextStyle.semiBold.copyWith(
                          fontSize: 14,
                          color: UIColors.green,
                        ),
                      ),
                      Text(
                        date,
                        style: UITextStyle.regular.copyWith(
                          fontSize: 13,
                          color: UIColors.grayText,
                        ),
                      ),
                    ],
                  ),
                  Row(
                    children: [
                      Text(
                        "Thu nhập:",
                        style: UITextStyle.regular.copyWith(
                          fontSize: 13,
                          color: UIColors.grayText,
                        ),
                      ),
                      const SizedBox(
                        width: 10,
                      ),
                      Text(
                        FormatUtil.currencyFormat(bonus),
                        style: UITextStyle.semiBold.copyWith(
                          fontSize: 14,
                          color: UIColors.green,
                        ),
                      ),
                    ],
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }
}
