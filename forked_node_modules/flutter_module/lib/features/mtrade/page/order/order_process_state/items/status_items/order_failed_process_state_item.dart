import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/routes/routes.gr.dart';

import '../../../../../../../common/colors.dart';
import '../../../../../../../common/styles.dart';
import '../../../../../../../common/utils/text_util.dart';
import '../../../../../../../common/widgets/buttons.dart';
import '../../../../../../../common/widgets/images.dart';
import '../../../../../../../models/mtrade/order/order_model.dart';
import '../order_process_state_mixin.dart';

class OrderFailedProcessStateItem extends StatelessWidget with OrderProcessStateMixin {
  const OrderFailedProcessStateItem({
    Key? key,
    required this.data,
    this.isDisable = false,
  }) : super(key: key);

  final OrderModel data;
  final bool isDisable;

  @override
  Widget build(BuildContext context) {
    final title = data.processText ?? "";
    final image = data.icon ?? "";
    final note = data.note ?? "";
    final date = convertDate(data.updatedDate);
    //
    return AppSplashButton(
      isDisable: isDisable,
      onTap: () {
        context.pushRoute(MTradeOrderDetailRoute(orderId: data.id));
      },
      child: SizedBox(
        height: 44,
        child: Row(
          children: [
            Align(
              alignment: Alignment.topLeft,
              child: AppImage.network(
                url: image,
                width: 24,
                height: 24,
              ),
            ),
            const SizedBox(
              width: 10,
            ),
            Expanded(
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Row(
                    mainAxisAlignment: MainAxisAlignment.spaceBetween,
                    children: [
                      Text(
                        title,
                        style: UITextStyle.semiBold.copyWith(
                          fontSize: 14,
                          color: UIColors.red,
                        ),
                      ),
                      Text(
                        date,
                        style: UITextStyle.regular.copyWith(
                          fontSize: 13,
                          color: UIColors.grayText,
                        ),
                      ),
                    ],
                  ),
                  Row(
                    children: [
                      Text(
                        "Ghi chú:",
                        style: UITextStyle.regular.copyWith(
                          fontSize: 13,
                          color: UIColors.grayText,
                        ),
                      ),
                      const SizedBox(
                        width: 10,
                      ),
                      Text(
                        note,
                        style: UITextStyle.regular.copyWith(
                          fontSize: 14,
                          color: UIColors.grayText,
                        ),
                      ),
                    ],
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }
}
