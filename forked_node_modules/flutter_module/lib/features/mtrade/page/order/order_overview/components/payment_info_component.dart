import 'package:flutter/material.dart';
import 'package:flutter_module/common/utils/format_util.dart';

import '../../../../../../common/colors.dart';
import '../../../../../../common/styles.dart';
import '../../../../../../models/mtrade/order/mtrade_pay_now_order_model.dart';

class PaymentInfoComponent extends StatelessWidget {
  const PaymentInfoComponent({super.key, this.order});

  final MTradePayNowOrderModel? order;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Row(
          children: [
            Text(
              'Phương thức:',
              style: UITextStyle.regular.copyWith(
                color: UIColors.grayText,
              ),
            ),
            const SizedBox(width: 4),
            Expanded(
              child: Text(
                order?.paymentDetail?.paymentMethod ?? '',
                textAlign: TextAlign.end,
                style: UITextStyle.regular,
              ),
            ),
          ],
        ),
        Row(
          children: [
            Text(
              'Thực thanh toán:',
              style: UITextStyle.regular.copyWith(
                color: UIColors.grayText,
              ),
            ),
            const SizedBox(width: 4),
            Expanded(
              child: Text(
                FormatUtil.currencyFormat(order?.paymentDetail?.paymentTotal),
                textAlign: TextAlign.end,
                style: UITextStyle.semiBold.copyWith(
                  fontSize: 16,
                  color: UIColors.red,
                ),
              ),
            ),
          ],
        ),
      ],
    );
  }
}