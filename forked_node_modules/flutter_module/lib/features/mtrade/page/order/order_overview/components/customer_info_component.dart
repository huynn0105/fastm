import 'package:ekyc/common/widgets/buttons.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/global_function.dart';
import 'package:flutter_module/common/utils/format_util.dart';

import '../../../../../../common/colors.dart';
import '../../../../../../common/styles.dart';
import '../../../../../../models/mtrade/order/mtrade_pay_now_order_model.dart';

class CustomerInfoComponent extends StatelessWidget {
  const CustomerInfoComponent({super.key, this.order});

  final MTradePayNowOrderModel? order;

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          'Thông tin người nhận:',
          style: UITextStyle.regular.copyWith(
            color: UIColors.grayText,
          ),
        ),
        const SizedBox(height: 2),
        Row(
          children: [
            Text(
              '• ${order?.paymentDetail?.customerInfo?.name?.toUpperCase() ?? ''} - ',
              style: UITextStyle.regular,
            ),
            SplashButton(
              onTap: () {
                GlobalFunction.launchPhone(order?.paymentDetail?.customerInfo?.phone ?? '');
              },
              child: Text(
                FormatUtil.phoneFormat(order?.paymentDetail?.customerInfo?.phone ?? ''),
                style: UITextStyle.semiBold.copyWith(
                  color: UIColors.primaryColor,
                ),
              ),
            ),
          ],
        ),
        Text(
          '• ${order?.paymentDetail?.customerInfo?.address ?? ''}',
          style: UITextStyle.regular,
        )
      ],
    );
  }
}
