import 'package:flutter/material.dart';

import '../../../../../../../../common/colors.dart';
import '../../../../../../../../common/styles.dart';
import '../../../../../../../../common/utils/format_util.dart';

class ScheduleDownPaymentItem extends StatelessWidget {
  const ScheduleDownPaymentItem({
    Key? key,
    required this.percent,
    required this.amount,
  }) : super(key: key);

  final double percent;
  final double amount;

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
      child: Row(
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: [
          Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text(
                "Trả trước ${FormatUtil.doubleFormat(percent)}% đơn hàng",
                style: UITextStyle.medium.copyWith(
                  fontSize: 16,
                ),
              ),
              Text(
                "Ngay sau khi ký hợp đồng\ntrả chậm thành công",
                style: UITextStyle.regular.copyWith(
                  fontSize: 14,
                  color: UIColors.secondaryColor,
                  fontStyle: FontStyle.italic,
                ),
              ),
            ],
          ),
          Text(
            FormatUtil.currencyDoubleFormat(amount),
            style: UITextStyle.medium.copyWith(
              fontSize: 16,
            ),
          ),
        ],
      ),
    );
  }
}
