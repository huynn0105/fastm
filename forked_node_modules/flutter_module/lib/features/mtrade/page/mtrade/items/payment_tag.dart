import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';

class PaymentTag extends StatelessWidget {
  const PaymentTag.now({
    Key? key,
    this.title = "Trả ngay",
    this.titleColor = UIColors.secondaryColor,
    this.backgroundColor = UIColors.lightSecondaryColor,
  }) : super(key: key);

  const PaymentTag.later({
    Key? key,
    this.title = "Trả chậm",
    this.titleColor = UIColors.primaryColor,
    this.backgroundColor = UIColors.lightBlue,
  }) : super(key: key);

  final String title;
  final Color titleColor;
  final Color backgroundColor;

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 20,
      width: 64,
      alignment: Alignment.center,
      // transform: Matrix4.skewX(-0.25),
      decoration: BoxDecoration(
        color: backgroundColor,
      ),
      child: Text(
        title,
        style: UITextStyle.medium.copyWith(
          fontSize: 12,
          height: 1,
          color: titleColor,
        ),
      ),
    );
  }
}
