import 'package:flutter/material.dart';

import '../../../../../common/widgets/images.dart';

class PromotionTag extends StatelessWidget {
  const PromotionTag({
    super.key,
    this.width = 76,
    this.height = 20,
  });

  final double width;
  final double height;

  @override
  Widget build(BuildContext context) {
    return AppImage.asset(
      asset: "ic_mtrade_promotion_ticket",
      width: width,
      height: height,
      fit: BoxFit.fill,
    );
  }
}
