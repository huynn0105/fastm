import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/utils/format_util.dart';
import 'package:flutter_module/common/widgets/images.dart';

import '../../../../../../../common/styles.dart';

class BonusItem extends StatelessWidget {
  const BonusItem({
    Key? key,
    required this.title,
    required this.asset,
    required this.price,
    required this.color,
  }) : super(key: key);

  final String title;
  final String asset;
  final int price;
  final Color color;

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 16),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            title,
            style: UITextStyle.regular.copyWith(
              fontSize: 14,
              color: UIColors.grayText,
            ),
          ),
          const SizedBox(
            height: 5,
          ),
          Row(
            children: [
              AppImage.asset(
                asset: asset,
                width: 24,
                height: 24,
              ),
              const SizedBox(
                width: 5,
              ),
              Expanded(
                child: Text(
                  FormatUtil.currencyFormat(price),
                  style: UITextStyle.semiBold.copyWith(
                    fontSize: 16,
                    color: color,
                  ),
                  maxLines: 1,
                  overflow: TextOverflow.ellipsis,
                ),
              ),
            ],
          ),
        ],
      ),
    );
  }
}

class HorizontalBonusItem extends StatelessWidget {
  const HorizontalBonusItem({
    Key? key,
    required this.title,
    required this.asset,
    required this.price,
    required this.color,
  }) : super(key: key);

  final String title;
  final String asset;
  final int price;
  final Color color;

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Expanded(
          child: Text(
            title,
            style: UITextStyle.regular.copyWith(
              fontSize: 14,
              color: UIColors.grayText,
            ),
            maxLines: 1,
            overflow: TextOverflow.ellipsis,
          ),
        ),
        const SizedBox(
          height: 5,
        ),
        AppImage.asset(
          asset: asset,
          width: 24,
          height: 24,
        ),
        const SizedBox(
          width: 5,
        ),
        Text(
          FormatUtil.currencyFormat(price),
          style: UITextStyle.semiBold.copyWith(
            fontSize: 16,
            color: color,
          ),
          maxLines: 1,
          overflow: TextOverflow.ellipsis,
        ),
      ],
    );
  }
}
