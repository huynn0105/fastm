import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:flutter_module/common/bloc_status.dart';

import '../../../../../../../common/colors.dart';
import '../../../../../../../common/styles.dart';
import '../../../../../cubit/product_group/mtrade_product_group_cubit.dart';

class GroupTitleItem extends StatelessWidget {
  const GroupTitleItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return BlocBuilder<MTradeProductGroupCubit, MTradeProductGroupState>(
      builder: (context, state) {
        return state.groupStatus.showLoading
            ? const SizedBox()
            : Padding(
                padding: const EdgeInsets.fromLTRB(16, 28, 16, 0),
                child: Text(
                  "Gợi ý hôm nay",
                  style: UITextStyle.semiBold.copyWith(
                    fontSize: 14,
                    color: UIColors.grayText,
                  ),
                  maxLines: 1,
                  overflow: TextOverflow.ellipsis,
                ),
              );
      },
    );
  }
}
