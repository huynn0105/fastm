import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/widgets/buttons.dart';
import 'package:flutter_module/models/mtrade/personal_website_setup/mtrade_contact_method_model.dart';

import '../../../../../common/utils/color_util.dart';
import '../../../../../common/widgets/images.dart';

class ContactMethodItem extends StatelessWidget {
  const ContactMethodItem({
    Key? key,
    required this.data,
    required this.onTap,
  }) : super(key: key);

  final ContactMethodModel data;
  final Function() onTap;

  @override
  Widget build(BuildContext context) {
    return SplashButton(
      onTap: onTap,
      borderRadius: BorderRadius.circular(28),
      child: Container(
        width: 56,
        height: 56,
        alignment: Alignment.center,
        decoration: BoxDecoration(
          color: ColorUtil.fromHex(data.background ?? ""),
          shape: BoxShape.circle,
          boxShadow:  const [
            BoxShadow(
              color: UIColors.shadow,
              offset: Offset(-5, 5),
              blurRadius: 10,
              spreadRadius: 0,
            ),
          ],
        ),
        child: AppImage.network(
          url: data.icon ?? "",
          width: 36,
          height: 36,
        ),
      ),
    );
  }
}
