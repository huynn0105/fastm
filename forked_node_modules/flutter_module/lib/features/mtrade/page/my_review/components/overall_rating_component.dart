import 'package:flutter/material.dart';
import 'package:flutter_module/common/utils/text_util.dart';
import 'package:flutter_module/features/mtrade/page/my_review/components/star_rating_widget.dart';
import 'package:flutter_module/models/mtrade/my_review/mtrade_filter_my_review_model.dart';

import '../../../../../../common/colors.dart';
import '../../../../../../common/styles.dart';

class OverallRatingComponent extends StatelessWidget {
  const OverallRatingComponent({
    Key? key,
    required this.avg,
    required this.amount,
    required this.percent,
  }) : super(key: key);
  final double avg;
  final int amount;
  final RateList? percent;

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.symmetric(
        horizontal: 16,
        vertical: 12,
      ),
      color: UIColors.white,
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            'Đánh giá và nhận xét về bạn',
            style: UITextStyle.medium.copyWith(
              color: UIColors.grayText,
            ),
          ),
          const SizedBox(
            height: 8,
          ),
          Row(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Column(
                children: [
                  const SizedBox(
                    height: 8,
                  ),
                  Text(
                    avg.toStringAsFixed(1).replaceAll('.', ','),
                    style: UITextStyle.bold.copyWith(
                      fontSize: 40,
                      color: UIColors.green,
                      height: 1,
                    ),
                  ),
                  const SizedBox(
                    height: 1,
                  ),
                  Text(
                    '/5 sao',
                    style: UITextStyle.regular.copyWith(
                      fontSize: 13,
                      color: UIColors.grayText,
                    ),
                  ),
                ],
              ),
              const SizedBox(
                width: 24,
              ),
              Expanded(
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.end,
                  children: [
                    StarRatingWidget(
                      rating: 5,
                      percent: TextUtils.parseDouble(percent?.s1) ?? 0,
                    ),
                    const SizedBox(
                      height: 2,
                    ),
                    StarRatingWidget(
                      rating: 4,
                      percent: TextUtils.parseDouble(percent?.s2) ?? 0,
                    ),
                    const SizedBox(
                      height: 2,
                    ),
                    StarRatingWidget(
                      rating: 3,
                      percent: TextUtils.parseDouble(percent?.s3) ?? 0,
                    ),
                    const SizedBox(
                      height: 2,
                    ),
                    StarRatingWidget(
                      rating: 2,
                      percent: TextUtils.parseDouble(percent?.s4) ?? 0,
                    ),
                    const SizedBox(
                      height: 2,
                    ),
                    StarRatingWidget(
                      rating: 1,
                      percent: TextUtils.parseDouble(percent?.s5) ?? 0,
                    ),
                    const SizedBox(
                      height: 4,
                    ),
                    Text(
                      "$amount đánh giá",
                      style: UITextStyle.regular.copyWith(
                        fontSize: 13,
                        color: UIColors.grayText,
                        height: 1,
                      ),
                    ),
                  ],
                ),
              ),
            ],
          ),
        ],
      ),
    );
  }
}
