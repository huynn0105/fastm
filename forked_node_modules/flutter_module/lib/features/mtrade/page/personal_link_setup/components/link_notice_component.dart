import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/images.dart';

class LinkNoticeComponent extends StatelessWidget {
  const LinkNoticeComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.symmetric(
        horizontal: 12,
        vertical: 10,
      ),
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(10),
        color: UIColors.lightSecondaryColor,
      ),
      child: Column(
        children: [
          Row(
            children: [
              const AppImage.asset(
                asset: 'ic_notification',
                width: 24,
                height: 24,
                color: UIColors.orange,
              ),
              const SizedBox(
                width: 8,
              ),
              Text(
                'Lưu ý',
                style: UITextStyle.bold.copyWith(
                  color: UIColors.orange,
                ),
              )
            ],
          ),
          const SizedBox(
            height: 8,
          ),
          Text(
            'Tất cả khách hàng để lại nhu cầu tham gia từ đường link của bạn đều được chuyển đến các chuyên viên tư vấn và xử lý tốt nhất của MFast',
            style: UITextStyle.regular.copyWith(
              fontSize: 13,
              color: UIColors.boolText,
            ),
          )
        ],
      ),
    );
  }
}
