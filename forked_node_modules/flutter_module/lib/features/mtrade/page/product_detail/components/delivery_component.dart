import 'package:flutter/material.dart';
import '../../../../../common/styles.dart';
import '../../../../../common/utils/text_util.dart';
import '../../../../../common/widgets/collect_data_view.dart';

import '../../../../../common/colors.dart';
import '../../mtrade/components/delivery_support/delivery_support_information_item.dart';

class DeliveryComponent extends StatelessWidget {
  const DeliveryComponent({
    Key? key,
    required this.errorMessage,
  }) : super(key: key);

  final String? errorMessage;

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16),
      decoration: const BoxDecoration(
        color: UIColors.white,
      ),
      child: CollectDataView(
        errorMsg: errorMessage,
        child: Row(
          children: [
            Text(
              "Giao đến",
              style: UITextStyle.medium.copyWith(
                fontSize: 16,
                color: UIColors.grayText,
              ),
            ),
            const SizedBox(
              width: 10,
            ),
            Expanded(
              child: DeliverySupportInformationItem(
                hasError: TextUtils.isNotEmpty(errorMessage),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
