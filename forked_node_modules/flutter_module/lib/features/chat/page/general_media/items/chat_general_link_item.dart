import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/buttons.dart';
import 'package:flutter_module/common/widgets/images.dart';
import 'package:flutter_module/features/chat/page/general_media/chat_general_media_page.dart';

class ChatGeneralLinkItem extends StatelessWidget {
  const ChatGeneralLinkItem({
    super.key,
    required this.item,
    this.onTap,
  });

  final GeneralMedia item;
  final Function()? onTap;

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(12),
      margin: const EdgeInsets.only(top: 8),
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(8),
        color: UIColors.white,
      ),
      child: Row(
        children: [
          Container(
            width: 48,
            height: 48,
            decoration: BoxDecoration(
              borderRadius: BorderRadius.circular(8),
              color: UIColors.extraLightBlue,
            ),
            child: const Center(
              child: AppImage.asset(
                asset: 'ic_link',
                width: 32,
                height: 32,
              ),
            ),
          ),
          const SizedBox(
            width: 16,
          ),
          Expanded(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text(
                  'Link mở thẻ BIDV',
                  style: UITextStyle.medium.copyWith(
                    fontSize: 16,
                    color: UIColors.boolText,
                  ),
                ),
                const SizedBox(
                  height: 4,
                ),
                Text(
                  'mfast.vn/zalopay/97124',
                  style: UITextStyle.regular.copyWith(
                    color: UIColors.primaryColor,
                  ),
                )
              ],
            ),
          ),
          SplashButton(
            onTap: () {
              onTap?.call();
            },
            child: const AppImage.asset(
              asset: 'ic_dots',
              width: 24,
              height: 24,
            ),
          )
        ],
      ),
    );
  }
}
