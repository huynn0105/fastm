enum MarkupSuggestionPosition {
  top,
  bottom,
}

extension ExMarkupSuggestionPosition on MarkupSuggestionPosition {
  bool get isTop => this == MarkupSuggestionPosition.top;

  bool get isBottom => this == MarkupSuggestionPosition.bottom;
}
