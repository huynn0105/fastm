// ignore_for_file: public_member_api_docs, sort_constructors_first
part of 'chat_add_member_cubit.dart';

class ChatAddMemberState extends Equatable {
  const ChatAddMemberState({
    this.status = BlocStatus.initial,
    this.usersSelected = const [],
  });

  final BlocStatus status;
  final List<ChatContactModel> usersSelected;
  @override
  List<Object> get props => [
        status,
        usersSelected,
      ];

  ChatAddMemberState copyWith({
    BlocStatus? status,
    List<ChatContactModel>? usersSelected,
  }) {
    return ChatAddMemberState(
      status: status ?? this.status,
      usersSelected: usersSelected ?? this.usersSelected,
    );
  }
}
