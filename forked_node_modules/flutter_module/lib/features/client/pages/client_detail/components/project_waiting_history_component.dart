import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/features/client/pages/client_detail/items/project_waiting_history_item.dart';
import 'package:flutter_module/models/client/client_detail/client_project_waiting_model.dart';

class ProjectWaitingHistoryComponent extends StatelessWidget {
  const ProjectWaitingHistoryComponent({
    super.key,
    required this.data,
  });

  final List<ClientProjectWaitingModel> data;

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisSize: MainAxisSize.min,
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          "Nhu cầu tham gia",
          style: UITextStyle.regular.copyWith(
            color: UIColors.grayText,
          ),
        ),
        const SizedBox(
          height: 8,
        ),
        ListView.separated(
          shrinkWrap: true,
          padding: EdgeInsets.zero,
          physics: const NeverScrollableScrollPhysics(),
          itemBuilder: (context, index) {
            return ProjectWaitingHistoryItem(
              data: data[index],
            );
          },
          separatorBuilder: (_, __) => const Divider(
            height: 20,
            indent: 56,
          ),
          itemCount: data.length,
        ),
      ],
    );
  }
}
