import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/images.dart';

class ClientTurnOrderWidget extends StatelessWidget {
  const ClientTurnOrderWidget.previous({
    super.key,
    this.asset = 'ic_arrow_left',
    this.label = 'TRƯỚC ĐÓ',
    this.crossAxisAlignment = CrossAxisAlignment.start,
  });

  const ClientTurnOrderWidget.next({
    super.key,
    this.asset = 'ic_arrow_right',
    this.label = 'TIẾP THEO',
    this.crossAxisAlignment = CrossAxisAlignment.end,
  });

  final String asset;
  final String label;
  final CrossAxisAlignment crossAxisAlignment;

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: crossAxisAlignment,
      children: [
        AppImage.asset(
          asset: asset,
          width: 24,
          height: 24,
        ),
        const SizedBox(
          height: 6,
        ),
        Text(
          'Khách hàng',
          style: UITextStyle.regular.copyWith(
            fontSize: 13,
            color: UIColors.grayText,
          ),
        ),
        const SizedBox(
          height: 3,
        ),
        Text(
          label,
          style: UITextStyle.bold.copyWith(
            fontSize: 15,
            color: UIColors.secondaryColor,
          ),
        ),
      ],
    );
  }
}
