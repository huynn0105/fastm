import 'package:auto_route/auto_route.dart';
import 'package:collection/collection.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/enum/client/client_project_status.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/utils/datetime_util.dart';
import 'package:flutter_module/common/utils/format_util.dart';
import 'package:flutter_module/common/utils/text_util.dart';
import 'package:flutter_module/common/widgets/buttons.dart';
import 'package:flutter_module/common/widgets/divider.dart';
import 'package:flutter_module/common/widgets/images.dart';
import 'package:flutter_module/models/client/client_detail/client_project_model.dart';
import 'package:flutter_module/models/client/client_detail/project_detail_model.dart';
import 'package:flutter_module/routes/routes.gr.dart';

class FinanceProjectDetailItem extends StatelessWidget {
  const FinanceProjectDetailItem({
    super.key,
    this.item,
    this.initItem,
  });

  final FinanceProjectDetailModel? item;
  final ClientProjectModel? initItem;

  @override
  Widget build(BuildContext context) {
    final status = ClientProjectStatus.values.firstWhereOrNull((e) => e.code == initItem?.status);

    return ListView(
      padding: EdgeInsets.zero,
      physics: const NeverScrollableScrollPhysics(),
      shrinkWrap: true,
      children: [
        Padding(
          padding: const EdgeInsets.all(16),
          child: Container(
            decoration: BoxDecoration(
              color: UIColors.white,
              borderRadius: BorderRadius.circular(8),
            ),
            padding: const EdgeInsets.all(16),
            child: Column(
              children: [
                Row(
                  children: [
                    AppImage.network(
                      url: item?.iconURL ?? "",
                      width: 64,
                      height: 40,
                      fit: BoxFit.contain,
                    ),
                    const SizedBox(
                      width: 16,
                    ),
                    Expanded(
                      child: Column(
                        crossAxisAlignment: CrossAxisAlignment.start,
                        children: [
                          Text(
                            "Sản phẩm",
                            style: UITextStyle.regular.copyWith(
                              color: UIColors.grayText,
                            ),
                          ),
                          Text(
                            item?.projectName ?? "",
                            style: UITextStyle.regular,
                          ),
                        ],
                      ),
                    )
                  ],
                ),
                const DottedDivider(
                  height: 12,
                  color: UIColors.lightGray,
                ),
                Row(
                  children: [
                    Expanded(
                      child: ItemValue(
                        title: "Khoản vay đề nghị",
                        value: item?.uwRequestAmount == null
                            ? "---"
                            : FormatUtil.currencyDoubleFormat(item?.uwRequestAmount),
                        isBold: true,
                      ),
                    ),
                    Expanded(
                      child: ItemValue(
                        title: "Khoản vay chấp nhận",
                        value: item?.uwApproveAmount == null
                            ? "---"
                            : FormatUtil.currencyDoubleFormat(item?.uwApproveAmount),
                        isBold: true,
                      ),
                    ),
                  ],
                ),
                const SizedBox(
                  height: 8,
                ),
                Row(
                  children: [
                    Expanded(
                      child: ItemValue(
                        title: "Số tháng vay đề nghị",
                        value: item?.uwTenureRequested == null ? "---" : item?.uwTenureRequested.toString() ?? "",
                        isBold: true,
                      ),
                    ),
                    Expanded(
                        child: ItemValue(
                      title: "Số tháng vay chấp nhận",
                      value: item?.uwTermApproved == null ? "---" : item?.uwTermApproved.toString() ?? "",
                      isBold: true,
                    )),
                  ],
                ),
                const SizedBox(
                  height: 8,
                ),
                Row(
                  children: [
                    Expanded(
                      child: ItemValue(
                        title: "Mã hợp đồng vay",
                        value: item?.uwContractNumber ?? "---",
                        isBold: true,
                      ),
                    ),
                    Expanded(
                      child: ItemValue(
                        title: "Sale Code",
                        value: item?.saleCode ?? "---",
                        isBold: true,
                        valueColor: item?.saleCode == null ? UIColors.boolText : UIColors.red,
                      ),
                    ),
                  ],
                ),
                const DottedDivider(
                  height: 8,
                  color: UIColors.lightGray,
                ),
                ItemValueRow(
                  title: "Tạo bởi",
                  value: item?.initFullName ?? "",
                ),
                const SizedBox(
                  height: 6,
                ),
                ItemValueRow(
                  title: "Trạng thái",
                  value: initItem?.statusText ?? "",
                  isBold: true,
                  valueColor: status?.color ?? UIColors.boolText,
                ),
                const SizedBox(
                  height: 6,
                ),
                ItemValueRow(
                  title: "Cập nhật cuối",
                  value: item?.lastProcessText ?? "",
                ),
                const SizedBox(
                  height: 6,
                ),
                ItemValueRow(
                  title: "Pega ID",
                  value: item?.appUID ?? "",
                ),
                const SizedBox(
                  height: 6,
                ),
                ItemValueRow(
                  title: "Vào lúc",
                  value: DateTimeUtil.getNumberDayBetween(
                    initItem?.updatedDate ?? '',
                    forceShowDateFormat: (duration) {
                      if (duration.inDays > 3) {
                        return DateTimeFormat.ddMMyyyy;
                      }
                      return null;
                    },
                  ),
                ),
              ],
            ),
          ),
        ),
        if (TextUtils.isNotEmpty(item?.webviewURL))
          Center(
            child: SplashButton(
              onTap: () {
                _onTapMore(context, url: item?.webviewURL);
              },
              child: Row(
                mainAxisSize: MainAxisSize.min,
                children: [
                  Text(
                    "Xem chi tiết",
                    style: UITextStyle.medium.copyWith(
                      fontSize: 16,
                      color: UIColors.primaryColor,
                    ),
                  ),
                  const SizedBox(
                    width: 4,
                  ),
                  const AppImage.asset(
                    asset: "ic_arrow_long_right",
                    width: 28,
                    height: 28,
                  ),
                ],
              ),
            ),
          ),
        const SizedBox(
          height: 12,
        ),
      ],
    );
  }

  _onTapMore(BuildContext context, {String? url}) {
    if (url == null) return;
    context.pushRoute(WebViewRoute(
      title: "Chi tiết dự án",
      url: url,
    ));
  }
}

class ItemValue extends StatelessWidget {
  const ItemValue({
    super.key,
    this.title = "",
    this.value = "",
    this.subValue,
    this.isBold = false,
    this.valueColor = UIColors.boolText,
  });

  final String title;
  final String value;
  final String? subValue;
  final bool isBold;
  final Color valueColor;

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text(
          title,
          style: UITextStyle.regular.copyWith(
            fontSize: 13,
            color: UIColors.grayText,
          ),
        ),
        const SizedBox(
          height: 2,
        ),
        RichText(
          text: TextSpan(
            style: (isBold ? UITextStyle.bold : UITextStyle.regular).copyWith(color: valueColor),
            children: [
              TextSpan(text: value),
              if (subValue != null)
                TextSpan(
                  text: "\n$subValue",
                  style: UITextStyle.regular,
                ),
            ],
          ),
        )
      ],
    );
  }
}

class ItemValueRow extends StatelessWidget {
  const ItemValueRow({
    super.key,
    this.title = "",
    this.value = "",
    this.subValue,
    this.isBold = false,
    this.valueColor = UIColors.boolText,
  });

  final String title;
  final String value;
  final String? subValue;
  final bool isBold;
  final Color valueColor;

  @override
  Widget build(BuildContext context) {
    return Row(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Expanded(
          flex: 35,
          child: Text(
            title,
            style: UITextStyle.regular.copyWith(
              fontSize: 13,
              color: UIColors.grayText,
            ),
          ),
        ),
        const SizedBox(
          height: 2,
        ),
        Expanded(
          flex: 65,
          child: RichText(
            text: TextSpan(
              style: (isBold ? UITextStyle.bold : UITextStyle.regular).copyWith(color: valueColor),
              children: [
                TextSpan(text: value),
                if (subValue != null)
                  TextSpan(
                    text: "\n$subValue",
                    style: UITextStyle.regular,
                  ),
              ],
            ),
          ),
        )
      ],
    );
  }
}
