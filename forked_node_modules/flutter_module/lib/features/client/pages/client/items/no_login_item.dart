import 'package:flutter/material.dart';
import 'package:flutter_module/features/personal_setting/pages/personal_setting/widgets/no_information_widget.dart';

import '../../../../../../common/colors.dart';
import '../../../../../../common/styles.dart';

class NoLoginItem extends StatelessWidget {
  const NoLoginItem({
    Key? key,
    required this.onLogin,
  }) : super(key: key);

  final VoidCallback onLogin;

  @override
  Widget build(BuildContext context) {
    return NoInformationWidget(
      titleColor: UIColors.darkBlue,
      asset: "ic_client_mfast",
      title: "",
      content: "Đăng nhập để quản lý khách hàng của bạn",
      titleButton: "Đăng nhập ngay",
      contentStyle: UITextStyle.medium.copyWith(
        fontSize: 16,
      ),
      onTap: onLogin,
    );
  }
}
