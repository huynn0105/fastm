import 'package:flutter/material.dart';

import '../../../../../common/widgets/block_widget.dart';

class WrapFilterItem extends StatelessWidget {
  const WrapFilterItem({
    Key? key,
    required this.title,
    required this.children,
  }) : super(key: key);

  final String title;
  final List<Widget> children;

  @override
  Widget build(BuildContext context) {
    return BlockWidget(
      title: title,
      child: Wrap(
        spacing: 8,
        runSpacing: 12,
        children: children,
      ),
    );
  }
}
