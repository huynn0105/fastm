import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/size.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/appbar.dart';
import 'package:flutter_module/common/widgets/buttons.dart';

import 'components/action_header.dart';
import 'components/filter_mentor.dart';
import 'components/search_bar.dart';
import 'items/mentor_item.dart';

@RoutePage()
class RegisterMentorPage extends StatelessWidget {
  const RegisterMentorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: MFastSimpleAppBar(
        title: "Chọn người dẫn dắt",
        context: context,
        actions: const [ActionHeader()],
      ),
      body: Container(
        margin: const EdgeInsets.symmetric(horizontal: 16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            const SizedBox(
              height: 16,
            ),
            const MentorSearchBar(),
            const SizedBox(
              height: 24,
            ),
            Text(
              'Đề xuất cho bạn',
              style: UITextStyle.medium.copyWith(
                fontSize: 16,
                color: UIColors.grayText,
              ),
            ),
            const SizedBox(
              height: 8,
            ),
            const FilterMentor(),
            const SizedBox(
              height: 16,
            ),
            Expanded(
              child: Container(
                padding: const EdgeInsets.symmetric(
                  horizontal: 12,
                ),
                decoration: BoxDecoration(
                  color: UIColors.white,
                  borderRadius: BorderRadius.circular(8),
                ),
                child: ListView.builder(
                  itemCount: 10,
                  itemBuilder: (context, index) {
                    return const MentorItem();
                  },
                ),
              ),
            ),
            const SizedBox(
              height: 28,
            ),
            Row(
              children: [
                Expanded(
                  child: AppOutlinedButton(
                    onPressed: () {
                      Navigator.of(context).pop();
                    },
                    backgroundColor: Colors.transparent,
                    borderRadius: BorderRadius.circular(50),
                    padding: const EdgeInsets.symmetric(
                      vertical: 12,
                    ),
                    title: "Quay lại",
                    textColor: UIColors.grayText,
                  ),
                ),
                const SizedBox(
                  width: 12,
                ),
                Expanded(
                  child: PrimaryButton(
                    onPressed: () {},
                    title: 'Chọn người này',
                    padding: const EdgeInsets.symmetric(
                      vertical: 12,
                    ),
                    radius: 50,
                  ),
                ),
              ],
            ),
            SizedBox(
              height: AppSize.instance.safeBottom,
            )
          ],
        ),
      ),
    );
  }
}
