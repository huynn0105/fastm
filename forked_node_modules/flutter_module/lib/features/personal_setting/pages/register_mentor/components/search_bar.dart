import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';

class MentorSearchBar extends StatelessWidget {
  const MentorSearchBar({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 48,
      child: TextField(
        decoration: InputDecoration(
          enabledBorder: OutlineInputBorder(
              borderRadius: BorderRadius.circular(32),
              borderSide: const BorderSide(color: UIColors.gray)),
          focusedBorder: OutlineInputBorder(
              borderRadius: BorderRadius.circular(32),
              borderSide: const BorderSide(color: UIColors.primaryColor)),
          contentPadding: EdgeInsets.zero,
          focusColor: UIColors.primaryColor,
          hintText: 'Tìm theo nickname hoặc mã MFast',
          hintStyle:
              UITextStyle.medium.copyWith(fontSize: 16, color: UIColors.gray),
          filled: true,
          fillColor: UIColors.white,
        ),
        style: UITextStyle.medium.copyWith(fontSize: 16),
        textAlign: TextAlign.center,
      ),
    );
  }
}
