import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import '../../widgets/no_information_widget.dart';

class NoMentorItem extends StatelessWidget {
  const NoMentorItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return NoInformationWidget(
      titleColor: UIColors.darkBlue,
      asset: "ic_no_mentor",
      title: "Bạn chưa có người dẫn dắt",
      content: "Người đứng ngay trên nhánh của bạn, do bạn tự chỉ định, sẽ trực tiếp hỗ trợ, dẫn dắt bạn trong suốt con đường Huyền thoại.",
      titleButton: "Chọn người dẫn dắt",
      onTap: () {},
    );
  }
}
