import 'package:flutter/material.dart';

import '../../../../../../common/colors.dart';
import '../../../../../../common/styles.dart';
import '../../widgets/no_information_widget.dart';

class NoLoginItem extends StatelessWidget {
  const NoLoginItem({
    Key? key,
    required this.onLogin,
  }) : super(key: key);

  final VoidCallback onLogin;

  @override
  Widget build(BuildContext context) {
    return NoInformationWidget(
      titleColor: UIColors.darkBlue,
      asset: "ic_not_login",
      title: "",
      content: "Đăng nhập để trở thành đại lý tài chính\ntất cả-trong-một cùng MFast.",
      titleButton: "Đăng nhập ngay",
      contentStyle: UITextStyle.bold.copyWith(
        fontSize: 16,
      ),
      onTap: onLogin,
    );
  }
}
