import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/images.dart';
import 'package:flutter_module/features/client/pages/client_detail/widgets/dialog/confirm_dialog.dart';
import 'package:flutter_module/models/personal_setting/bank_account_model.dart';

class DialogRemoveBankAccountComponent extends StatelessWidget {
  const DialogRemoveBankAccountComponent({
    super.key,
    this.onConfirm,
    this.confirmTittleButton,
    this.bankAccount,
  });

  final VoidCallback? onConfirm;
  final String? confirmTittleButton;
  final BankAccountModel? bankAccount;

  @override
  Widget build(BuildContext context) {
    return ClientConfirmDialog(
      positiveTitle: 'Xem lại',
      negativeTitle: 'Hủy liên kết',
      negativeColor: UIColors.red,
      positiveColor: UIColors.primaryColor,
      onNegative: () {
        onConfirm?.call();
      },
      onPositive: () {
        Navigator.of(context).pop();
      },
      child: Column(
        children: [
          const SizedBox(
            height: 20,
          ),
          const AppImage.asset(
            asset: "ic_notification",
            width: 56,
            height: 56,
            color: UIColors.darkBlue,
          ),
          const SizedBox(
            height: 12,
          ),
          Padding(
            padding: const EdgeInsets.symmetric(horizontal: 16),
            child: Text(
              "Xác nhận hủy liên kết tài khoản: ${bankAccount?.bankName} - ${bankAccount?.bankFullName} - ${bankAccount?.bankAccountNumber}",
              style: UITextStyle.semiBold.copyWith(
                fontSize: 16,
                color: UIColors.darkBlue,
              ),
              textAlign: TextAlign.center,
            ),
          ),
          const SizedBox(
            height: 12,
          ),
          Padding(
            padding: const EdgeInsets.symmetric(horizontal: 16),
            child: Text(
              'Sau khi hủy liên kết, thông tin về tài khoản này sẽ được xóa khỏi MFast và không thể khôi phục.',
              style: UITextStyle.regular.copyWith(
                fontSize: 14,
                color: UIColors.grayText,
              ),
              textAlign: TextAlign.center,
            ),
          ),
          const SizedBox(
            height: 8,
          ),
          const SizedBox(
            height: 16,
          ),
        ],
      ),
    );
  }
}
