import 'package:flutter/material.dart';
import 'package:flutter_module/common/enum/income/history_income_status.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/utils/text_util.dart';

import '../../../../../../../common/widgets/images.dart';

class HistoryStatusItem extends StatelessWidget {
  const HistoryStatusItem({
    Key? key,
    required this.status,
  }) : super(key: key);

  final HistoryIncomeStatus? status;

  @override
  Widget build(BuildContext context) {
    if (status == null) {
      return const SizedBox();
    }
    return Container(
      padding: const EdgeInsets.symmetric(vertical: 4, horizontal: 8),
      decoration: BoxDecoration(
        color: status!.backgroundColor,
        borderRadius: BorderRadius.circular(16),
      ),
      child: RichText(
        text: TextSpan(
          children: [
            TextSpan(
              text: status!.label,
              style: UITextStyle.medium.copyWith(
                fontSize: 12,
                height: 1,
                color: status!.color,
              ),
            ),
            const WidgetSpan(
              child: SizedBox(
                width: 2,
              ),
            ),
            WidgetSpan(
              child: AppImage.asset(
                asset: status!.asset,
                width: 16,
                height: 16,
                color: status!.color,
                fit: BoxFit.contain,
              ),
            ),
          ],
        ),
      ),
      // child: Row(
      //   mainAxisSize: MainAxisSize.min,
      //   crossAxisAlignment: CrossAxisAlignment.center,
      //   children: [
      //     if (TextUtils.isNotEmpty(status!.label)) ...[
      //       Flexible(
      //         child: Text(
      //           status!.label,
      //           style: UITextStyle.medium.copyWith(
      //             fontSize: 13,
      //             height: 1,
      //             color: status!.color,
      //           ),
      //         ),
      //       ),
      //       const SizedBox(
      //         width: 2,
      //       ),
      //     ],
      //     AppImage.asset(
      //       asset: status!.asset,
      //       width: 20,
      //       height: 20,
      //       fit: BoxFit.contain,
      //     ),
      //   ],
      // ),
    );
  }
}
