import 'package:flutter/material.dart';

import '../items/warning_item.dart';

class UnavailableTransactionComponent extends StatelessWidget {
  const UnavailableTransactionComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const WarningItem(
      title: "Hiện tại, hệ thống đang chạy đối soát nên việc rút tiền tạm thời không khả dụng.",
      asset: "ic_lock",
      showHelper: true,
    );
  }
}
