import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/dialogs/dialog_view/mtrade/mtrade_dialog_view.dart';
import 'package:flutter_module/routes/routes.gr.dart';

class EKYCIncomeComponent extends StatelessWidget {
  const EKYCIncomeComponent({
    super.key,
    this.message,
  });

  final String? message;

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.only(top: 50),
      margin: const EdgeInsets.symmetric(horizontal: 16),
      child: MTradeDialogView(
        asset: 'ic_mtrade_mascot_ekyc',
        message: message ?? 'Bạn cần định danh tài khoản trước\nđể sử dụng tính năng này',
        messageAlign: TextAlign.center,
        showDottedDivider: false,
        messageColor: UIColors.defaultText,
        positiveTitle: 'Định danh tài khoản ngay',
        enableAutoPop: false,
        positiveCallback: () {
          context.router.replace(const MFastAccountIdentificationRoute());
        },
      ),
    );
  }
}
