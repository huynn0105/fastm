import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/utils/format_util.dart';
import 'package:flutter_module/models/income/tax_holding_model.dart';

class TaxHoldingItem extends StatelessWidget {
  const TaxHoldingItem({
    super.key,
    this.data,
  });

  final TaxHoldingModel? data;

  @override
  Widget build(BuildContext context) {
    final title = data?.title ?? '';
    final totalAmount = data?.totalAmount ?? 0;
    final totalHolding = data?.totalTaxHolding ?? 0;
    final elapsedTime = data?.lastUpdate ?? '';

    ///
    return Container(
      padding: const EdgeInsets.all(12),
      child: Column(
        mainAxisSize: MainAxisSize.min,
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            title,
            style: UITextStyle.regular.copyWith(
              fontSize: 13,
            ),
          ),
          Row(
            children: [
              Expanded(
                flex: 3,
                child: Text(
                  "Tạm giữ: ",
                  style: UITextStyle.regular.copyWith(
                    fontSize: 12,
                    color: UIColors.grayText,
                  ),
                ),
              ),
              const SizedBox(
                width: 16,
              ),
              Expanded(
                flex: 4,
                child: Container(
                  alignment: Alignment.centerLeft,
                  child: Text(
                    FormatUtil.currencyDoubleFormat(totalHolding).replaceAll("-", "- "),
                    style: UITextStyle.semiBold.copyWith(
                      fontSize: 13,
                      color: UIColors.red,
                    ),
                  ),
                ),
              ),
              const Expanded(
                flex: 4,
                child: SizedBox(),
              ),
            ],
          ),
          Row(
            children: [
              Expanded(
                flex: 3,
                child: Text(
                  "Tổng thu nhập: ",
                  style: UITextStyle.regular.copyWith(
                    fontSize: 12,
                    color: UIColors.grayText,
                  ),
                ),
              ),
              const SizedBox(
                width: 16,
              ),
              Expanded(
                flex: 4,
                child: Container(
                  alignment: Alignment.centerLeft,
                  child: Text(
                    FormatUtil.currencyDoubleFormat(totalAmount),
                    style: UITextStyle.semiBold.copyWith(
                      fontSize: 13,
                      color: UIColors.darkBlue,
                    ),
                  ),
                ),
              ),
              Expanded(
                flex: 4,
                child: Align(
                  alignment: Alignment.centerRight,
                  child: Text(
                    elapsedTime,
                    style: UITextStyle.semiBold.copyWith(
                      fontSize: 12,
                      color: UIColors.grayText,
                    ),
                  ),
                ),
              ),
            ],
          ),
        ],
      ),
    );
  }
}
