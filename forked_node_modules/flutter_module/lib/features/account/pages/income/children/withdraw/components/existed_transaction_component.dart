import 'package:flutter/material.dart';

import '../items/warning_item.dart';

class ExistedTransactionComponent extends StatelessWidget {
  const ExistedTransactionComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const WarningItem(
      title: "Hiện tại, bạn có 1 giao dịch đang xử lý nên tính năng rút tiền sẽ tạm khóa cho đến khi GD này hoàn tất",
      asset: "ic_notification",
      showHelper: false,
    );
  }
}
