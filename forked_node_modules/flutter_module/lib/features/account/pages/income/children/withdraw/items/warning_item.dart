import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/images.dart';

class WarningItem extends StatelessWidget {
  const WarningItem({
    Key? key,
    required this.title,
    required this.asset,
    this.showHelper = false,
  }) : super(key: key);

  final String title;
  final String asset;
  final bool showHelper;

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(12),
      decoration: BoxDecoration(
        color: UIColors.lightRed,
        border: Border.all(
          color: UIColors.red,
        ),
        borderRadius: BorderRadius.circular(8),
      ),
      child: Row(
        crossAxisAlignment: CrossAxisAlignment.baseline,
        textBaseline: TextBaseline.alphabetic,
        children: [
          AppImage.asset(
            asset: asset,
            width: 28,
            height: 28,
            color: UIColors.red,
          ),
          const SizedBox(
            width: 10,
          ),
          Expanded(
            child: Column(
              children: [
                Text(
                  title,
                  style: UITextStyle.semiBold,
                ),
                const SizedBox(
                  height: 5,
                ),
                Visibility(
                  visible: showHelper,
                  child: RichText(
                    text: TextSpan(
                      children: [
                        TextSpan(
                          text: "Vui lòng liên hệ tổng đài ",
                          style: UITextStyle.regular.copyWith(
                            fontSize: 13,
                          ),
                        ),
                        TextSpan(
                          text: "0899.909.789",
                          style: UITextStyle.bold.copyWith(
                            fontSize: 13,
                            color: UIColors.primaryColor,
                          ),
                        ),
                        TextSpan(
                          text: " để biết thêm chi tiết. Mong bạn thông cảm vì sự bất tiện này nhé!",
                          style: UITextStyle.regular.copyWith(
                            fontSize: 13,
                          ),
                        ),
                      ],
                    ),
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }
}
