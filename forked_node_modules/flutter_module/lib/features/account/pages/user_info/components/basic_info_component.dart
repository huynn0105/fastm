import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/enum/mfast/gender_type.dart';
import 'package:flutter_module/features/personal_setting/pages/personal_setting/widgets/personal_setting_block_widget.dart';
import 'package:flutter_module/models/user/user_info_model.dart';
import 'package:flutter_module/models/user/user_meta_data_model.dart';
import 'package:flutter_module/routes/routes.gr.dart';

import '../../../../../common/widgets/buttons.dart';
import '../../../../../common/widgets/images.dart';
import '../items/user_info_item.dart';

class BasicInfoComponent extends StatelessWidget {
  const BasicInfoComponent({
    Key? key,
    required this.userMetaData,
  }) : super(key: key);

  final UserMetaDataModel? userMetaData;

  @override
  Widget build(BuildContext context) {
    return PersonalSettingBlockWidget(
      title: "Thông tin cơ bản",
      margin: const EdgeInsets.all(16),
      padding: const EdgeInsets.all(12),
      trailing: AppSplashButton(
        onTap: () {
          context.pushRoute(const UpdateBasicUserInfoRoute());
        },
        child: const AppImage.asset(
          asset: "ic_edit",
          width: 24,
          height: 24,
        ),
      ),
      child: Column(
        children: [
          UserInfoItem(
            title: "Giới tính",
            content: userMetaData?.getGenderType().isMale == true ? 'Nam' : 'Nữ',
          ),
          const SizedBox(
            height: 10,
          ),
          UserInfoItem(
            title: "Email",
            content: userMetaData?.emailAddress ?? '',
          ),
          const SizedBox(
            height: 10,
          ),
          UserInfoItem(
            title: "Huyện / Tỉnh",
            content: userMetaData?.addressCurrentDistrict ?? '',
          ),
          const SizedBox(
            height: 10,
          ),
          UserInfoItem(
            title: "Địa chỉ liên hệ",
            content: userMetaData?.addressCurrent ?? '',
          ),
        ],
      ),
    );
  }
}
