import 'package:flutter_module/common/widgets/buttons.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/images.dart';

class IntroPassCodeComponent extends StatelessWidget {
  const IntroPassCodeComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: UIColors.white,
      padding: const EdgeInsets.symmetric(horizontal: 16),
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          const SizedBox(height: 16),
          Padding(
            padding: const EdgeInsets.symmetric(horizontal: 32),
            child: Text(
              'Vui lòng đăng nhập bằng OTP trước, sau đó kích hoạt chức năng này để sử dụng cho các lần đăng nhập sau',
              textAlign: TextAlign.center,
              style: UITextStyle.regular.copyWith(
                fontSize: 15,
                color: UIColors.boldText,
              ),
            ),
          ),
          const SizedBox(height: 16),
          const Row(
            children: [
              Expanded(
                child: AppImage.asset(asset: 'introduce_passcode1'),
              ),
              SizedBox(width: 12),
              Expanded(
                child: AppImage.asset(asset: 'introduce_passcode2'),
              )
            ],
          ),
          const SizedBox(height: 42),
          PrimaryButton(
            width: double.infinity,
            title: 'Đã hiểu và quay lại',
            onPressed: () {
              Navigator.pop(context);
            },
          ),
          const SizedBox(height: 42),
        ],
      ),
    );
  }
}
