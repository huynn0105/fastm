part of '../onboarding_page.dart';

class _PageIntroView extends StatelessWidget {
  const _PageIntroView({
    super.key,
    required this.title,
    required this.subtitle,
    required this.image,
  });

  final String title;
  final String subtitle;
  final String image;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        SizedBox(
          height: 200,
          child: Column(
            children: [
              Text(
                title,
                style: UITextStyle.extraBold.copyWith(
                  fontSize: 32,
                  color: UIColors.blue,
                  fontWeight: FontWeight.w900,
                ),
                textAlign: TextAlign.center,
              ),
              const SizedBox(height: 8),
              Text(
                subtitle,
                style: UITextStyle.semiBold.copyWith(
                  fontSize: 16,
                  color: Colors.black,
                ),
                textAlign: TextAlign.center,
              ),
            ],
          ),
        ),
        AppImage.asset(
          asset: image,
          width: double.infinity,
        ),
      ],
    );
  }
}
