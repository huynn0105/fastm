import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/dialogs/dialog_view/mtrade/mtrade_dialog_view.dart';
import 'package:flutter_module/common/widgets/buttons.dart';

class NoInternetComponent extends StatelessWidget {
  const NoInternetComponent({
    super.key,
    required this.onTap,
  });

  final Function() onTap;

  @override
  Widget build(BuildContext context) {
    return Container(
      color: UIColors.background,
      padding: const EdgeInsets.only(top: 50, left: 16, right: 16),
      child: Column(
        children: [
          const MTradeDialogView(
            asset: "ic_mtrade_mascot_waiting",
            title: "Lỗi kết nối với mạng internet",
            message:
                "Rất tiếc, đang có lỗi kết nối với mạng internet. Vui lòng kiểm tra đường truyền Wifi hoặc mạng 4G/5G và thử lại sau.",
            titleColor: UIColors.secondaryColor,
            messageAlign: TextAlign.left,
            showDottedDivider: true,
          ),
          PrimaryButton(
            onPressed: onTap,
            title: "Tải lại trang",
          ),
        ],
      ),
    );
  }
}
