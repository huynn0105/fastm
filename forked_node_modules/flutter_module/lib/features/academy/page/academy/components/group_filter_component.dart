import 'package:flutter/material.dart';

class GroupFilterComponent extends StatelessWidget {
  const GroupFilterComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
