import 'package:auto_route/annotations.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/utils/text_util.dart';
import 'package:flutter_module/features/news/pages/news/components/enable_tracking_alert_component.dart';
import 'package:flutter_module/features/news/pages/news/components/tabbar_component.dart';
import '../../../../common/widgets/appbar.dart';

@RoutePage()
class NewsPage extends StatelessWidget {
  const NewsPage({
    super.key,
    @QueryParam('focusedTabIndex') this.focusedTabIndex,
    @QueryParam('tabNewsIndex') this.newsTabIndex,
  });

  final String? focusedTabIndex;
  final String? newsTabIndex;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: MFastSimpleAppBar(
        title: "Thông tin",
        context: context,
      ),
      body: Column(
        children: [
          const EnableTrackingAlertComponent(),
          Expanded(
            child: TabbarComponent(
              focusedTabIndex: TextUtils.parseInt(focusedTabIndex),
              newsTabIndex: TextUtils.parseInt(newsTabIndex),
            ),
          ),
        ],
      ),
    );
  }
}
