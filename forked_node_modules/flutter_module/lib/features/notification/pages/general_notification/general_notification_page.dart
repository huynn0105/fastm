import 'package:auto_route/annotations.dart';
import 'package:flutter/material.dart';

import 'components/filter_component.dart';
import 'components/news_component.dart';

@RoutePage()
class GeneralNotificationPage extends StatelessWidget {
  const GeneralNotificationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          FilterComponent(),
          NewsComponent(),
        ],
      ),
    );
  }
}
