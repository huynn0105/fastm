import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/widgets/appbar.dart';
import 'package:flutter_module/routes/routes.gr.dart';

import '../../../../common/widgets/tabbar/tabbar.dart';

@RoutePage()
class NotificationPage extends StatelessWidget {
  const NotificationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AutoTabsRouter.tabBar(
      routes: const [
        FluctuationNotificationRoute(),
        GeneralNotificationRoute(),
      ],
      builder: (context, child, tabController) {
        return Scaffold(
          appBar: MFastAdvanceAppBar.setting(
            context: context,
            title: "Trung tâm tin tức",
            backgroundColor: UIColors.white,
          ),
          body: Column(
            children: [
              Container(
                decoration: const BoxDecoration(
                  boxShadow: [
                    BoxShadow(
                      color: Colors.black38,
                      spreadRadius: 2,
                    ),
                  ],
                ),
                child: UnderlineIndicatorTabbar(
                  controller: tabController,
                  height: 45,
                  titles: const [
                    "Biến động",
                    "Thông báo",
                  ],
                ),
              ),
              Expanded(
                child: child,
              ),
            ],
          ),
        );
      },
    );
  }
}
