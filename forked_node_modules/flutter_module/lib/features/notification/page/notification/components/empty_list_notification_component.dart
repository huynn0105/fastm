import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/enum/mfast/notification_tab_type.dart';
import 'package:flutter_module/common/size.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/images.dart';

class EmptyListNotificationComponent extends StatelessWidget {
  const EmptyListNotificationComponent({
    super.key,
    this.type,
  });

  final String? type;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        const SizedBox(
          height: 84,
        ),
        if (type == NotificationTabType.flags.id) ...[
          SizedBox(
            width: AppSize.instance.width * 0.8,
            child: const AspectRatio(
              aspectRatio: 307 / 166,
              child: AppImage.asset(
                asset: 'noti_flag_ex',
              ),
            ),
          ),
        ] else ...[
          const AppImage.asset(
            asset: 'ic_empty_noti',
            width: 120,
            height: 90,
          ),
          // ignore: prefer_const_constructors
          SizedBox(
            height: 16,
          ),
          Text(
            "Không có tin tức nào được tìm thấy",
            style: UITextStyle.regular.copyWith(
              color: UIColors.grayText,
            ),
          )
        ]
      ],
    );
  }
}
