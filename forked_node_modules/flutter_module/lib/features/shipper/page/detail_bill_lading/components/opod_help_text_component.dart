import 'package:flutter/material.dart';
import 'package:flutter_module/common/enum/mtrade/payment_type.dart';
import 'package:flutter_module/common/widgets/images.dart';

import '../../../../../common/colors.dart';
import '../../../../../common/styles.dart';

class OPODHelpTextComponent extends StatelessWidget {
  const OPODHelpTextComponent({
    super.key,
    required this.paymentTypeCode,
  });

  final String paymentTypeCode;

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(8),
        color: UIColors.lightSecondaryColor,
      ),
      padding: const EdgeInsets.all(8),
      child: Column(
        children: [
          Text(
            'Đơn hàng ${paymentTypeCode == MTradePaymentType.later.code ? 'trả chậm' : 'trả ngay'} / Giao hàng OPOD',
            style: UITextStyle.regular.copyWith(
              color: UIColors.grayText,
            ),
          ),
          Row(
            mainAxisAlignment: MainAxisAlignment.center,
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              const AppImage.asset(
                asset: 'ic_warning',
                width: 24,
                height: 24,
              ),
              const SizedBox(
                width: 6,
              ),
              SizedBox(
                width: 212,
                child: Text(
                  paymentTypeCode == MTradePaymentType.later.code ? 'Thanh toán online số tiền phải\ntrả trước lúc nhận hàng' : 'Thanh toán online lúc nhận hàng',
                  textAlign: TextAlign.center,
                  style: UITextStyle.bold.copyWith(
                    color: UIColors.secondaryColor,
                  ),
                ),
              ),
            ],
          )
        ],
      ),
    );
  }
}
