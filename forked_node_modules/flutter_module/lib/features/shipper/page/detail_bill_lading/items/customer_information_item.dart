import 'package:flutter/material.dart';
import 'package:flutter_module/common/utils/text_util.dart';
import '../../../../../common/styles.dart';

import '../../../../../common/colors.dart';
import '../../../../../models/shipper/order/shipper_detail_order_model.dart';
import '../widgets/phone_number_widget.dart';

class CustomerInformationItem extends StatelessWidget {
  const CustomerInformationItem({Key? key, this.data}) : super(key: key);
  final DeliveryModel? data;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        SizedBox(
          height: 50,
          width: double.infinity,
          child: Stack(
            clipBehavior: Clip.none,
            alignment: AlignmentDirectional.topCenter,
            children: [
              Positioned(
                top: -35,
                child: Container(
                  padding: const EdgeInsets.all(2),
                  decoration: const BoxDecoration(
                    color: UIColors.white,
                    shape: BoxShape.circle,
                  ),
                  child: Container(
                    width: 80,
                    height: 80,
                    decoration: const BoxDecoration(
                      color: UIColors.lightBlue,
                      shape: BoxShape.circle,
                    ),
                    child: Center(
                      child: Text(
                        TextUtils.getLastCharOfName(data?.orderDetail?.omsCustomer?.name ?? ''),
                        style: UITextStyle.bold.copyWith(
                          fontSize: 32,
                          color: UIColors.primaryColor,
                        ),
                      ),
                    ),
                  ),
                ),
              ),
            ],
          ),
        ),
        const SizedBox(
          height: 12,
        ),
        Text(
          data?.orderDetail?.omsCustomer?.name ?? '',
          style: UITextStyle.semiBold.copyWith(
            fontSize: 18,
          ),
        ),
        const SizedBox(
          height: 4,
        ),
        PhoneNumberWidget(
          value: data?.orderDetail?.omsCustomer?.phone ?? '',
          showPhoneNumber: true,
        ),
      ],
    );
  }
}
