import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/utils/map_utils.dart';
import 'package:flutter_module/common/widgets/buttons.dart';
import 'package:flutter_module/common/widgets/images.dart';

import '../../../../../models/shipper/order/shipper_detail_order_model.dart';

class DeliveryAddressItem extends StatelessWidget {
  const DeliveryAddressItem({Key? key, this.data}) : super(key: key);
  final DeliveryModel? data;

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Row(
          children: [
            const AppImage.asset(
              asset: "ic_location_outline",
              width: 20,
              height: 20,
            ),
            const SizedBox(
              width: 8,
            ),
            Text(
              "ĐỊA CHỈ",
              style: UITextStyle.regular.copyWith(
                fontSize: 14,
                color: UIColors.grayText,
              ),
            ),
            const Spacer(),
            SplashButton(
              onTap: () {
                String? lat  = data?.orderDetail?.omsCustomer?.latitude;
                String? lng = data?.orderDetail?.omsCustomer?.longitude;
                MapUtils.openMap(context, lat, lng);
              },
              child: Row(
                mainAxisSize: MainAxisSize.min,
                children: [
                  Text(
                    "Chỉ đường",
                    style: UITextStyle.regular.copyWith(
                      fontSize: 14,
                      color: UIColors.primaryColor,
                    ),
                  ),
                  const SizedBox(
                    width: 4,
                  ),
                  const AppImage.asset(
                    asset: "ic_shipper_introduction_arrow",
                    width: 24,
                    height: 24,
                    color: UIColors.primaryColor,
                  ),
                ],
              ),
            ),
          ],
        ),
        Padding(
          padding: const EdgeInsets.only(left: 28),
          child: Text(
            data?.orderDetail?.omsCustomer?.addressDelivery ?? '',
            style: UITextStyle.regular.copyWith(
              fontSize: 14,
            ),
          ),
        ),
      ],
    );
  }
}
