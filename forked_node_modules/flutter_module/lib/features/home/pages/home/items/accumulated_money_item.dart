import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/widgets/buttons.dart';
import 'package:flutter_module/routes/routes.gr.dart';

import '../../../../../common/colors.dart';
import '../../../../../common/styles.dart';
import '../../../../../common/widgets/images.dart';

class AccumulatedMoneyItem extends StatelessWidget {
  const AccumulatedMoneyItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SplashButton(
      onTap: () {
        context.pushRoute(IncomeRoute());
      },
      borderRadius: BorderRadius.circular(8),
      child: Container(
        height: 50,
        padding: const EdgeInsets.symmetric(horizontal: 16),
        decoration: BoxDecoration(
          color: UIColors.white,
          borderRadius: BorderRadius.circular(8),
          boxShadow: const [
            BoxShadow(
              color: Color(0x140b0b36),
              offset: Offset(0, 8),
              blurRadius: 12,
              spreadRadius: 0,
            ),
          ],
        ),
        child: Row(
          mainAxisAlignment: MainAxisAlignment.spaceBetween,
          children: [
            Text(
              "Tiền tích luỹ",
              style: UITextStyle.regular.copyWith(
                fontSize: 14,
              ),
            ),
            RichText(
              text: TextSpan(
                text: "8.000.000",
                style: UITextStyle.bold.copyWith(
                  fontSize: 17,
                  color: UIColors.primaryColor,
                ),
                children: [
                  TextSpan(
                    text: " đ ",
                    style: UITextStyle.regular.copyWith(
                      fontSize: 17,
                      color: UIColors.primaryColor,
                    ),
                  ),
                  const WidgetSpan(
                    child: AppImage.asset(
                      asset: "ic_arrow_right",
                      width: 20,
                      height: 20,
                    ),
                  ),
                ],
              ),
            ),
          ],
        ),
      ),
    );
  }
}
