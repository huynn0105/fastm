import 'package:auto_route/auto_route.dart';
import 'package:flutter/material.dart';
import 'package:flutter_module/common/widgets/buttons.dart';
import 'package:flutter_module/routes/routes.gr.dart';

import '../../../../../common/colors.dart';
import '../../../../../common/widgets/images.dart';

class OptionButtonsItem extends StatelessWidget {
  const OptionButtonsItem({
    Key? key,
    required this.width,
    required this.opacity,
  }) : super(key: key);

  final double width;
  final double opacity;

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        SplashButton(
          onTap: () {
            context.pushRoute(const FluctuationNotificationRoute());
          },
          borderRadius: BorderRadius.circular(20),
          child: Container(
            height: 40,
            width: 40,
            alignment: Alignment.center,
            decoration: BoxDecoration(
              color: UIColors.white.withOpacity(opacity),
              shape: BoxShape.circle,
            ),
            child: const AppImage.asset(
              asset: "ic_white_logo_mfast",
              height: 24,
              width: 24,
            ),
          ),
        ),
        SizedBox(
          width: width,
        ),
        SplashButton(
          onTap: () {
            context.pushRoute(const GeneralNotificationRoute());
          },
          borderRadius: BorderRadius.circular(20),
          child: Container(
            height: 40,
            width: 40,
            decoration: BoxDecoration(
              color: UIColors.white.withOpacity(opacity),
              shape: BoxShape.circle,
            ),
            child: const Icon(
              Icons.notifications_rounded,
              size: 24,
              color: UIColors.white,
            ),
          ),
        ),
      ],
    );
  }
}
