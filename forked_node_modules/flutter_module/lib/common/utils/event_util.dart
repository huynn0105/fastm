

class EventUtil {
  EventUtil._();

  static sendEventToRN(String eventName, {String? data}) {
    // getItInstance.get<BridgeService>().sendEvent(eventName, data: data);
  }

  static sendEventToRNv2(String eventName, {Map? data}) {
    final payload = {
      "key": eventName,
      "data": data,
    };

    // getItInstance.get<BridgeService>().sendEvent("FLUTTER_EVENT", data: jsonEncode(payload));
  }
}
