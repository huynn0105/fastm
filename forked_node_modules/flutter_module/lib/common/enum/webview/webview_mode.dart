// ignore_for_file: constant_identifier_names

enum WebViewScreenMode {
  NORMAL(0),
  STACK(1);

  const WebViewScreenMode(this.id);
  final int id;
}

enum WebViewBackMode {
  NONE,
  HIDDEN,
  NORMAL,
  CUSTOM,
  CUSTOM_URL,
}

enum WebViewCloseMode {
  NONE,
  NORMAL,
}
