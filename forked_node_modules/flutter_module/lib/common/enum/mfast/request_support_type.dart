enum RequestSupportType {
  pending,
  success,
  error;
}

extension RequestSupportTypeExtension on RequestSupportType {
  bool get isPending => this == RequestSupportType.pending;
  bool get isSuccess => this == RequestSupportType.success;
  bool get isError => this == RequestSupportType.error;
}
