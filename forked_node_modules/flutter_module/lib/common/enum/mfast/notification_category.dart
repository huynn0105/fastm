enum NotificationCategory {
  system,
  admin;
}

extension NotificationCategoryExtension on NotificationCategory {
  bool get isSystem => this == NotificationCategory.system;
  bool get isAdmin => this == NotificationCategory.admin;
}
