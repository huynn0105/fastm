import 'package:collection/collection.dart';

enum ReportItemType {
  money,
  count;

  static ReportItemType fromValue(jsonValue) =>
      ReportItemType.values.singleWhereOrNull((i) => jsonValue == i.name) ?? count;
}