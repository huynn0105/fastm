// ignore_for_file: constant_identifier_names

enum ClientTabType {
  all("all", "Tất cả"),
  priority("priority", "Tiềm năng"),
  pl("pl", "Vay tín chấp"),
  credit("credit", "Thẻ tín dụng"),
  mpl("mpl", "Bán hàng trả chậm"),
  daa("daa", "TK ngân hàng / Ví điện tử"),
  insurance("insurance", "Bảo hiểm");

  const ClientTabType(this.name, this.title);

  final String name;
  final String title;
}

enum ClientDetailType {
  PAGE_QC("page_qc"),
  TRASH("trash"),
  PRIORITY("priority");

  const ClientDetailType(this.code);
  final String code;
}
