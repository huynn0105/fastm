enum VehicleType {
  company(0, 'Xe công ty', 'personal'),
  personal(0, 'Xe cá nhân', 'organization');

  final int value;
  final String name;
  final String code;
  const VehicleType(this.value, this.name, this.code);
}