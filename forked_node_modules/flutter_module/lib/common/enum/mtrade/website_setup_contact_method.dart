enum WebsiteSetupContactMethod {
  socialPhone("Gọi trực tiếp vào SĐT trên"),
  socialZalo("Liên hệ qua Zalo với SĐT trên"),
  socialFacebook("Liên hệ qua Messenger");

  final String label;

  const WebsiteSetupContactMethod(this.label);
}

enum WebsiteTabPage {
  setup,
  statistic;
}