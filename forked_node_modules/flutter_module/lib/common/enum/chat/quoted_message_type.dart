enum QuotedMessageType {
  edit,
  reply,
  forward,
}

extension ExQuotedMessageType on QuotedMessageType {
  bool get isEdit => this == QuotedMessageType.edit;

  bool get isReply => this == QuotedMessageType.reply;

  bool get isForward => this == QuotedMessageType.forward;
}
