enum SupportRequestType {
  all("ALL", 'Tất cả'),
  pending("no_reply", 'Chờ trả lời'),
  responded("reply", "Đã trả lời"),
  closed('closed', "Đã đóng");

  final String value;
  final String title;
  const SupportRequestType(this.value, this.title);
}
