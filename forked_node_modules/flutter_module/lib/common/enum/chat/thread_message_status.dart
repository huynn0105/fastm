enum ThreadMessageStatus {
  sent,
  error,
  sending;
}

extension ExThreadMessageStatus on ThreadMessageStatus {
  bool get isSent => this == ThreadMessageStatus.sent;

  bool get isError => this == ThreadMessageStatus.error;

  bool get isSending => this == ThreadMessageStatus.sending;
}
