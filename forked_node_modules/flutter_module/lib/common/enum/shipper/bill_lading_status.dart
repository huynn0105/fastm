enum BillLadingType {
  newOrder("ORDER_NEW"),
  shippingOrder("ORDER_SHIPPING"),
  completedOrder("ORDER_COMPLETE");

  final String value;
  const BillLadingType(this.value);
}

enum BillLadingTabbar {
  newTab,
  deliveryTab,
  completedTab;
}