import 'package:flutter/widgets.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/styles.dart';
import 'package:flutter_module/common/widgets/buttons.dart';
import 'package:flutter_module/common/widgets/images.dart';

class MascotNoLoginWidget extends StatelessWidget {
  const MascotNoLoginWidget({
    super.key,
    required this.onLogin,
  });

  final VoidCallback onLogin;

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        const AppImage.asset(
          asset: 'ic_not_login',
          width: 156,
          height: 156,
        ),
        const SizedBox(
          height: 16,
        ),
        RichText(
          textAlign: TextAlign.center,
          text: TextSpan(
            style: UITextStyle.medium.copyWith(
              fontSize: 16,
              color: UIColors.defaultText,
            ),
            children: [
              const TextSpan(
                text: 'Đăng nhập để trở thành đại lý tài chính\n',
              ),
              TextSpan(
                text: 'tất cả-trong-một ',
                style: UITextStyle.bold.copyWith(
                  fontSize: 16,
                  color: UIColors.darkBlue,
                ),
              ),
              const TextSpan(
                text: 'về tài chính, bảo hiểm',
              ),
            ],
          ),
        ),
        const SizedBox(
          height: 16,
        ),
        PrimaryButton(
          onPressed: onLogin,
          title: 'Đăng nhập ngay',
          padding: const EdgeInsets.symmetric(
            vertical: 10,
            horizontal: 20,
          ),
          radius: 50,
        ),
      ],
    );
  }
}
