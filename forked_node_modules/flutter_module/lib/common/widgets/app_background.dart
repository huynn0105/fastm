import 'package:flutter/material.dart';
import 'package:flutter_module/common/colors.dart';
import 'package:flutter_module/common/widgets/images.dart';

import '../constants.dart';
import 'widget_layout.dart';

class AppBackground extends StatelessWidget {
  const AppBackground({
    super.key,
    required this.logo,
  });

  final String logo;

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: const BoxDecoration(
        gradient: LinearGradient(
          colors: [Color(0xFF221db0), Color(0xFF7071d2), Color(0xFF979be4), Color(0xFFd6deff)],
          begin: FractionalOffset(0.5, 1),
          end: FractionalOffset(0.5, -1),
          // stops: [0.0, 1.0],
          tileMode: TileMode.clamp,
        ),
      ),
      child: Stack(
        children: [
          Positioned.fill(
            child: WidgetLayout(
              child: Container(
                color: UIColors.extraLightGray,
              ),
            ),
          ),
          Padding(
            padding: const EdgeInsets.only(top: 24, left: 32, right: 32),
            child: AppImage.asset(
              asset: logo,
              width: 107,
              height: 26,
              fit: BoxFit.contain,
            ),
          ),
          Row(
            children: [
              const Spacer(),
              Column(
                children: const [
                  Spacer(
                    flex: 3,
                  ),
                  AppImage.asset(
                    asset: "mfast2_icon",
                    width: 94,
                    height: 94,
                  ),
                  Spacer(
                    flex: 1,
                  ),
                ],
              ),
              const Spacer(),
              const SizedBox(
                width: AppConstants.responsiveSize,
              ),
              const Spacer(),
              Column(
                children: const [
                  Spacer(
                    flex: 1,
                  ),
                  AppImage.asset(
                    asset: "mfast1_icon",
                    width: 120,
                    height: 120,
                  ),
                  Spacer(
                    flex: 3,
                  ),
                ],
              ),
              const Spacer(),
            ],
          ),
        ],
      ),
    );
  }
}
